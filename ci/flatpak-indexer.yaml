koji_config: brew
redis_url: redis://localhost:6379

registries:
  ghcr:
    public_url: https://ghcr.io/
    datasource: direct
    repositories:
      - gmanka-flatpaks/helloworld

indexes:
  latest:
    output: pages/index/static
    registry: ghcr
    # "" means all architectures in flatpak-indexer.
    architecture_expand: [""]
    tag: latest
    extract_icons: false
